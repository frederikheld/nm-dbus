{
  "name": "nm-dbus",
  "version": "0.1.0",
  "description": "Provides simplified access to NetworkManager features via DBus",
  "main": "index.js",
  "scripts": {
    "test": "mocha --exit tests/*.test.js",
    "test:dev": "mocha --watch \"tests/*.test.js\"",
    "test:coverage": "nyc mocha --exit tests/*.test.js"
  },
  "keywords": [
    "network-manager",
    "dbus",
    "networking",
    "linux"
  ],
  "author": "Frederik S. Held (kontakt@frederikheld.de)",
  "license": "MIT",
  "dependencies": {
    "dbus-next": "^0.9.2"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^8.3.2",
    "nyc": "^15.1.0"
  }
}
